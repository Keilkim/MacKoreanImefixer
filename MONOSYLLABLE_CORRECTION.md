# 단음절 교정 — 설계 기록

영자판 상태로 친 한글 **한 음절**(`fh`→로, `sms`→는)을 교정하는 계층의 근거와 계약.
이 문서의 모든 수치는 저장소·OS 자산으로 직접 실행해 얻은 실측값이다.
재현: `python3 -B scripts/lexicon/make_mono_lexicon.py surface`.

## 1. 무엇이 문제였나

사용자가 지목한 7개는 서로 **다른 두 지점**에서 죽고 있었다.

| 키열 | 한글 | 죽던 곳 |
|---|---|---|
| `fh` `dy` `dk` `sp` `dh` | 로 요 아 네 오 | `HangulStructure.isStrongKoreanCandidate` — R-K4가 단음절에 3키 이상을 요구해 엔진이 판정 자체를 포기(`weakKoreanStructure`) |
| `fmf` `sms` | 를 는 | `LexicalGuard` — 엔진은 교정을 냈으나 "라틴에 aeiou 가 없으면 약어"라는 무모음 게이트가 거부 |

두벌식에서 라틴 모음 `a·e·i·o·u`는 각각 자모 `ㅁ·ㄷ·ㅑ·ㅐ·ㅕ`다. 그래서 흔한 조사·대명사
단음절 대부분이 "라틴 모음 0개"가 되어 두 번째 게이트에 걸렸다.

## 2. 무모음 게이트는 안전 장치가 아니었다

옛 게이트를 실측하면 방향이 반대다.

| | 키열 수 | 그중 영어 사전 단어 |
|---|---:|---:|
| 3키+ 단음절 중 **무모음** (게이트가 **거부**) | 1,208 | **0** |
| 3키+ 단음절 중 **유모음** (게이트가 **통과**) | 435 | 2 |

무모음은 위험 표지가 아니라 **가장 안전한 칸의 표지**였다. 게이트가 통과시키던 435개 중
265개는 19,895어절 코퍼스에 한 번도 나오지 않는 음절이고, 그 안에 실제 파괴가 살아 있었다.

- `cma` → **츰** — CMA(증권 계좌). 라틴 모음 `a`가 있어 게이트를 그냥 통과했다.
- `aht` → **못**, `dha` → **옴** — 둘 다 영어 사전 표제어인데 교정되고 있었다.

즉 이 작업은 "안 되던 것을 되게 하는" 변경인 동시에 **이미 파괴 중이던 것을 막는** 변경이다.

## 3. 해법 — 구조 추정을 열거로 바꾼다

무모음 조건을 **삭제**하고, 영→한 결과가 한글 정확히 한 음절인 **모든** 결정에 대해
동결 자산(`scripts/lexicon/ko-mono.v1.txt`, 511행) 확인을 요구한다.

### 왜 구조가 아니라 목록인가

2키로 음절을 만드는 경로는 초성1+중성1 하나뿐이라 후보 전체가 구조 특징 한 점에 모인다 —
자모 모양 100% C+V, 종성 0, 최대 자음 연쇄 1. `dk`(아)와 `sk`(SK)는 자판 행·자모 모양·키
수·종성 유무가 전부 같고 초성 하나만 다르다. `sms`(는)와 `sns`(눈)는 인접한 두 키 차이다.
열거 가능한 어떤 구조 신호로도 분리되지 않으며, 남는 변수는 자모의 정체 = **어휘 정보**뿐이다.

그래서 이 계층은 판정하지 않는다. **조회만 한다.**

### 자산은 손 목록이 아니라 두 투영의 합집합

| | 행 | 출처 | 사람 판단 |
|---|---:|---|---|
| **KEEP** | 422 | 동결 엔진이 **오늘 실제로 시행하는** 단음절 교정의 투영 | **0** |
| **RESCUE** | 89 | 문법적 폐쇄류(T1)·유한 열거 부류(T2) 선언의 `ko_to_keys` 투영 | 한글 132개 선언 |

KEEP이 필요한 이유는 관문이 단음절 도메인 **전체**에 걸리기 때문이다. 관문을 통과할 자격을
자산이 유일하게 정의하므로, 오늘의 동작을 회귀 없이 유지하려면 오늘의 동작이 자산에 명시돼
있어야 한다. 이건 "이 교정이 옳다"는 승인이 아니라 **"오늘 이대로다"의 동결**이다.

`make_lexicon.py`가 쓰는 "무손실성" 논증은 여기서 원리적으로 성립하지 않는다 —
`ko_words.txt` 19,894행에 **1음절 어절이 0개**라 줄일 원본이 없다. 그래서 정당화를 위
두 축으로 대체하고, `verify`는 무손실성 대신 **정합성·도달성·안전성·경계** 10개 불변식을 본다.

### 허용은 한글로, 거부는 키열로 — 비대칭이 안전 축이다

사람이 답하는 질문은 정확히 둘이고 서로 다른 파일에 산다.

| 파일 | 질문 | 적는 것 |
|---|---|---|
| `mono-source.ko.tsv` | "이 한글이 이 부류의 구성원인가" | **한글만.** 키열은 스크립트가 계산 |
| `mono-veto.tsv` | "이 라틴 키열이 실사용 토큰인가" | **키열.** 19행 |

허용 방향에 키열을 직접 적을 수 있으면 누구든 아래 게이트를 우회해 항목을 밀어 넣을 수 있다.
거부 방향은 반대다 — 추가가 언제나 단조 안전하므로 키열로 적는 것이 옳다.

빈도·가치 판단은 어느 파일에도 적지 않는다. 있으면 그건 손 목록이다.

### 정지 조건

부류마다 `#bound` 상한을 선언하고 `verify` 불변식 8이 그 숫자를 강제한다. 목록을 키우려면
상한 한 줄을 고쳐야 하고, 그 diff가 리뷰 트리거가 된다. T2(감탄사·부사·용언활용 — 닫혀
있지 **않은** 부류)는 폐쇄류인 척하지 않고 격리해 총합 60으로 따로 묶는다.

## 4. 게이트 스택 — 각 게이트를 빼면 무엇이 통과하는가

| # | 게이트 | 시점 | 빼면 통과하는 것 |
|---|---|---|---|
| **G1** | 전대문자 = 약어 (R-D3) | 동결 엔진 | `SMS DNS XML CMA SNS` 등 대문자 표기 약어 전부. 이 게이트 덕분에 잔여 위험이 **소문자 표기로만** 좁혀진다 |
| **G2** | 자산 관문 | 런타임 (단일 통로) | 엔진 판정 그대로 **1,643개**. `Rnpa`→뀀, `rhkfa`→괆 등 빈도 0 음절 다수 |
| **G3** | **EN** — `/usr/share/dict/words` 투영 | 생성 시점 | `go`→해 `so`→내 `do`→애 `to`→새 `an`→무 `eh`→도 `sh`→노 `th`→소 `ro`→개 `di`→야 `en`→두 `sus`→년 `sha`→놈 `rhe`→곧 `wha`→좀 `Eh`→또 **`aht`→못 `dha`→옴**. 영어 문장 중 `go `가 **해**로 파괴된다 |
| **G4** | **CLI** — Apple 4개 bin 투영 | 생성 시점 | **`rm`→그, `du`→여.** G3로는 원리적으로 못 막는다 — 이들은 web2에 없다. `rm -rf` 앞의 `rm`이 **그**가 된다 |
| **G5** | **LOC** — `/usr/share/locale` 투영 | 생성 시점 | **`sk`→나** (SK텔레콤·하이닉스). G3·G4 어느 쪽에도 안 걸린다 |
| **G6** | 거부 원장 (19행) | 생성 시점 | **`cma`→츰**, `dl`→이(HTML `<dl>`), `dp`→에(DisplayPort), `tl`→시(TL;DR), `ch`→초, `wp`→제, `dml`→의, `gh`→호(GitHub CLI), `dir`→약. **G3~G5를 전부 통과하는 부류** — 사전에도 Apple 명령어에도 로케일에도 없다 |
| **G7** | 진단 대조 + 동결 정책 재실행 | 런타임 | 키열 사본이 발산했을 때 **엉뚱한 토큰**에 자산이 적용된다. 실제로 사본이 문장 전체로 자라 세 계층이 사코드였던 회귀 이력이 있다 |
| **G8** | 동결 조합기 교차확인 | 런타임 (2키 경로) | 자산 오타·NFD 혼입이 그대로 화면에 나간다. 있으면 파괴에 **두 독립 동결 근거의 동시 오류**가 필요하다 |
| **G9** | tier `.medium` + 6초 강조 칩 + ⌘Z | UI | 오교정을 알아채고 되돌릴 창이 좁아진다. `.medium` 6초는 `undoLifetime`과 정확히 같아 Undo 창 **전체**를 덮는다 |
| **G10** | fail-closed (자산 `nil`) | 런타임 | 자산 누락 시 동작이 정의되지 않는다. 지금은 단음절 교정이 **전부** 꺼져 오늘보다 엄격해진다 |

기각한 게이트 하나: **구조적 영어 veto**(`english_eval`의 `ok && cost==0`)를 필터로 쓰면
안 된다. 사용자 대표 사례 `dy`(요)가 `english_eval("dy") = (True, 0)`이라 걸린다.
음소배열 veto ≠ 사전 veto — 단음절 도메인에서는 후자만 안전하다.

## 5. 최악의 결과와 그것이 불가능한 이유

**최악**: 사용자가 의도한 영어·약어가 뜻 없는 한글 한 음절로 바뀌고, 되돌리지 못하는 것.

성립하려면 G1~G10이 **전부 동시에** 뚫려야 한다. 따라서 최악의 경우에도:

- 바뀔 수 있는 문자열은 **자산에 적힌 511개 음절뿐**이다. 다음절·낱자모·임의 텍스트는 이
  계층이 만들 수 없다 — 출력이 자산 값 그대로이고 조합기와의 등호로 고정돼 있다.
- 삭제 범위는 `originalCharacterCount`(2~5자)로 상한이 잡힌다.
- 6초 안의 ⌘Z가 텍스트와 **입력 소스**를 함께 복원한다.

그리고 **이 변경은 오늘보다 위험을 줄인다.** 오늘 관문을 통과할 수 있는 키열은 435개이고
판정 근거는 "라틴에 aeiou가 있는가"다. 변경 후에는 511개이고 근거는 **해시로 동결된 목록**이다.
개수는 늘지만 판정 근거가 휴리스틱에서 열거로 바뀌고, `cma`·`aht`·`dha`는 오늘 파괴하고
변경 후에는 파괴하지 않는다.

유일하게 남는 진짜 위험은 **"자산에 잘못된 행이 들어가는 것"**이며, 그것은 코드가 아니라
리뷰의 문제다. 그래서 `surface` 상한(CI)과 `audit` 출력(커밋 메시지)이 강제된다.

## 6. 이 변경이 여는 것과 닫는 것

**여는 것 — 신규 79개** (라벨만 승격된 10개는 오늘도 교정 중이라 제외)

- 갈래 A (2키, R-K4가 막던 것) **26개**:
  `fh`→로 `dy`→요 `dk`→아 `sp`→네 `dh`→오 `rk`→가 `tj`→서 `tn`→수 `sj`→너 `wj`→저
  `tp`→세 `ek`→다 `ej`→더 `dj`→어 `dn`→우 `gk`→하 `gn`→후 `gp`→헤 `wk`→자 `eo`→대
  `Eo`→때 `Rp`→께 `dO`→얘 `rO`→걔 `wO`→쟤 `dP`→예
- 갈래 B (3키+, 무모음 가드가 막던 것) **53개**:
  `fmf`→를 `sms`→는 `dmf`→을 `dms`→은 `gks`→한 `rjt`→것 `qjs`→번 `qns`→분 `rht`→곳
  `dlf`→일 `wnf`→줄 `Whr`→쪽 `dnjs`→원 `dnjf`→월 `dks`→안 `wkf`→잘 등

**닫는 것 — 3개**: `cma`→츰, `aht`→못, `dha`→옴. 전부 오늘 실제로 파괴 중이던 것.

**회귀 0**: `ehl`→되(빈도 646), `ehd`→동(243), `gka`→함(139), `qus`→변, `rma`→금,
`xla`→팀, `tkfa`→삶, `Wma`→쯤 등 오늘 동작하는 422개는 한 개도 바뀌지 않는다.
`verify` 불변식 3(KEEP 완전성)과 `MonosyllableLexiconTests.testKeepRowsCoverTodaysLiveCorrections`가
양쪽에서 강제한다.

## 7. 영구히 포기하는 것 — 선언했으나 게이트가 이긴 23자

거부는 항상 이긴다. 그 확정 비용을 숨기지 않고 적는다.

> **2026-07-25 정정.** 이 표는 오래 실제와 달랐다. `도`(eh) `야`(di) `해`(go)
> `개`(ro) `또`(Eh) 등은 `mono-admit.tsv` 로 이미 열려 있었고, `님`(sla) `놈`(sha)
> `곧`(rhe) `좀`(wha) `꽤`(Rho) 는 admit 에 적혀 있는데도 **다른 이유로** 빠져
> 있었다 — 생성기가 `ambiguousBothValid` 를 LexicalTiebreaker 로 넘겼는데, 그쪽
> 자산에는 1음절이 0개라 받을 곳이 없었다. 그 보류를 걷어내면서 함께 열렸다.
> `년`(sus) 은 현대 영어 "that's sus" 와 겹치고 결과가 욕설로 읽힐 수 있어
> admit 에서 되돌렸다. 지금 이 표에 남은 것은 실제로 EN 게이트가 닫는 3개뿐이다.

| 사유 | 한글 (키열) |
|---|---|
| **EN** — 영어 사전 표제어 | 두(`en`) 년(`sus`) 못(`aht`) |
| **CLI** — Apple 기본 명령어 | 그(`rm`) 여(`du`) |
| **LOC** — 로케일 코드 | 나(`sk`) |
| **원장** — 소문자 약어·서드파티 CLI | 이(`dl`) 의(`dml`) 에(`dp`) 시(`tl`) 초(`ch`) 제(`wp`) 호(`gh`) |

`이`·`의`·`에`·`도`·`그`·`나`는 아프다. 전부 최고빈도 조사·대명사다. 그러나 각각
`dl`(HTML `<dl>`)·`dml`(DML)·`dp`(DisplayPort)·`eh`·`rm`·`sk`와 정면 충돌하고, 이 계층의
원칙은 "파괴 방지 > 교정 놓침"이다. **되돌릴 때 코드 변경은 0줄이다** — 원장에서 한 줄
지우거나 소스에 한 줄 더하고 `generate` + freeze 갱신이면 된다.

## 8. 판단으로 남긴 것 (자동 판정이 아니다)

- **`sms`(는) admit.** 대문자 `SMS`는 R-D3가 이미 보존하므로 잔여 위험은 "소문자 `sms`
  단독 토큰" 하나로 좁혀지고, 그 빈도는 **미측정**이다. `는`은 사용자 대표 사례이며
  코퍼스 음절빈도 1위다. 철회는 `mono-veto.tsv` 한 줄 추가로 끝난다.
- **`-ㅁ` 약어 충돌군 7개 admit.** 두벌식에서 받침 ㅁ은 라틴 `a`로 끝나므로 `-ㅁ` 음절
  전체가 3글자 대문자 약어 네임스페이스와 **구조적으로** 충돌한다. 자산 511행을 영어 사전 ·
  `$PATH` 명령 · 약어 목록으로 재감사했을 때 걸리는 것이 정확히 이 부류다:
  `dma`(음) `rma`(금) `Rma`(끔) `wma`(즘) `Wma`(쯤) `xla`(팀) `tpa`(셈).

  **7개 전부 오늘 이미 교정되고 있다** — 이 변경이 새로 연 것이 하나도 없다. 닫으려면
  그만한 근거가 필요한데 ① 전대문자는 R-D3가 지키고 ② 한국어 쪽이 전부 실단어이며
  ③ 소문자 실사용 빈도를 잰 코퍼스가 없다. `cma`만 닫는 이유는 그것만 **한국어 쪽 비용이
  0**(츰, 코퍼스 빈도 0)이기 때문이다.

  같은 재감사에서 **이번 변경이 새로 연 위험은 `sms`(는) 하나뿐**이고, 그것은 위 항목의
  의도된 판단이다. 재현: `MONOSYLLABLE_CORRECTION.md` §11의 자산을 영어 사전·`$PATH`·
  약어 집합과 교차하면 8건이 나오고 그중 `sms`만 부류가 `keep`이 아니다.
- **약어의 실사용 빈도는 저장소·OS 어디서도 잴 수 없다.** `dns sns xml css api sql npm ssh`는
  web2에 없다. 원장 19행은 **판단**이며 사유코드가 그 사실을 표시한다. 다만 거부 방향이라
  판단이 틀려도 손해는 "교정을 놓침"이다.

## 9. 정직한 미해결

1. **KEEP 422행 중 262행은 코퍼스 빈도 0인 음절이다.** 닫고 싶지만 닫을 수 없다 —
   `ko_words.txt`는 단어성 오라클이 아니고, 빈도 0 목록에는 `봄`·`삶`·`쯤`·`둬`처럼 명백한
   실단어가 섞여 있다. 빈도로 닫으면 **측정된 손실을 미측정 이득과 바꾸는** 것이 된다.
   `audit`이 매번 전량을 출력한다. 단음절 한국어 코퍼스 확보가 후속 과제다.
2. **`ambiguousBothValid` 분기는 의도적 보류.** 주인은 `LexicalTiebreaker` 하나여야 한다.
   `만`(`aks`)·`들`(`emf`)·`등`(`emd`)·`듯`(`emt`)·`편`(`vus`)·`돼`(`eho`)·`백`(`qor`)·
   `열`(`duf`)·`명`(`aud`)·`딴`(`Eks`)·`몇`(`auc`)이 여기 있고, `ko-lexicon.v1.txt`에
   1음절 항목이 0개라 오늘 도달 불가다. `verify` 불변식 9가 두 자산의 키 교집합 = ∅ 와
   1음절 값 0개를 강제해, 나중에 그 자산이 자라도 관문이 tiebreaker 판정을 죽이지 못하게 한다.
3. **2키 토큰의 AX cold-start 취약성.** `maximumSoftProbeRefusalKeys`(2)가 토큰 길이와
   무관하므로 2키 토큰은 실패 시 회복할 뒤 키가 없다. 파괴가 아니라 **미발동**이라 허용하고,
   `preserved rule=weakKoreanStructure len=2` 로그로 관측 가능하게 둔다. 별도 과제.

## 10. R4-1 동결과의 관계

이 계층은 동결 엔진 **밖**에 있고, `scripts/engine-freeze.sha256`의 31파일을 한 바이트도
건드리지 않는다. 특히:

- **R-K4는 유효하고 그대로다.** 2키 하한은 무작위 충돌률 7.69%라는 구조적 근거로 세워진 것이고,
  그 근거는 지금도 옳다 — 구조만으로는 `dk`(아)와 `sk`(SK)를 가를 수 없다. 이 계층은 R-K4의
  **판정을 바꾸지 않고**, 그것이 보존한 토큰에 대해 동결 밖에서 어휘 근거를 댄다.
- `LayoutCorrectionPolicy.Rule`에 case를 추가하지 않았다. 2키 구제는 `.weakKoreanStructure`를
  그대로 쓰는데, 동결 정책은 이 rule을 **`.preserve`로만** 내므로 `(교정됨, rule=weak)` 조합은
  이 계층 말고는 만들 수 없다 — 위장 없이 로그에서 유일하게 식별된다.
- `HangulStructure.evaluate`는 **읽기 전용으로 호출**만 한다(G8 교차확인).

## 11. 관련 파일

| 파일 | 역할 |
|---|---|
| `scripts/lexicon/mono-source.ko.tsv` | 한글 선언 132행 + `#bound` (사람) |
| `scripts/lexicon/mono-veto.tsv` | 거부 원장 19행 (사람) |
| `scripts/lexicon/mono-gates.v1.txt` | OS 게이트 스냅샷 — `verify`를 결정적으로 만든다 |
| `scripts/lexicon/ko-mono.v1.txt` | 최종 자산 511행 (생성물) |
| `scripts/lexicon/make_mono_lexicon.py` | `generate` / `verify` / `surface` / `audit` |
| `Corpus/lexical/v1/monosyllable.tsv` | 파리티 fixture (511 correct + 40 preserve) |
| `Mackor/Mackor/MonosyllableLexicon.swift` | 조회 계층 |
| `Mackor/Mackor/LexicalGuard.swift` | 관문이 사는 곳 |
| `Mackor/Mackor/EventTapManager.swift` | 2키 구제 배선 (`monosyllableDecision`) |
| `Mackor/MackorTests/MonosyllableLexiconTests.swift` | 자산 불변식 12개 |
