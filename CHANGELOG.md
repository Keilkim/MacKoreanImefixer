# 변경 기록

Mackor의 사용자에게 보이는 변경 사항을 이 파일에 기록합니다. 내부 리팩터링처럼 사용자 동작에 영향이 없는 변경은 생략할 수 있습니다.

형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/)를 따르고, 버전은 [Semantic Versioning](https://semver.org/lang/ko/)을 기준으로 관리합니다.

## [Unreleased]

## [1.9] — 2026-07-25

### 변경

- **새 버전이 나오면 메뉴에서 바로 보입니다.** 지금까지는 업데이트가 있어도 설정 화면에 들어가야 확인할 수 있었습니다. 이제 새 버전을 찾으면 패널 아래쪽에 `업데이트 v1.9`가 버전 번호와 함께 나타납니다. 새 버전이 없을 때는 아무것도 표시하지 않습니다. 직접 확인하고 싶으면 `설정 → 업데이트 확인…`이 그대로 있습니다.

  업데이트는 이전과 같이 **직접 눌러야** 설치됩니다. 확인만 하루 한 번 자동으로 하고, 무인 설치는 하지 않습니다.

## [1.8] — 2026-07-25

### 변경

- **자주 쓰는 한 글자가 대폭 늘었습니다.** `좀`·`만`·`돼`·`들`·`온`·`첫`·`길`·`값`·`통`·`판`·`축`·`뻔`처럼 일상적으로 쓰는 한 글자 522자가 새로 교정됩니다(606 → 1,128자). 지금까지는 어떤 글자를 교정할지 사람이 부류별로 손으로 적어 왔고, 그 목록이 다 차면 흔한 말도 그냥 빠졌습니다. 이제 **macOS에 이미 들어 있는 한국어 사전**이 그 판단을 대신합니다. 단어를 하나씩 추가한 것이 아니라 판단 방식을 바꾼 것이라, 앞으로도 목록을 손보지 않고 늘어납니다.

  안전장치는 오히려 늘었습니다. 기존 영어 사전에 **macOS 영어 사전**을 더해 두 사전이 서로의 빈틈을 메웁니다 — 예전 사전이 모르던 `dmg`·`db`·`co` 같은 현대 약어를 새 사전이 잡아 줍니다. 또 두 글자짜리는 아예 열지 않고(`db`·`ep`·`co`처럼 약어가 빽빽한 영역), 검토를 거쳐 `cls`·`chr`·`dps`·`tld`처럼 개발자가 실제로 치는 44개를 따로 막았습니다.

- **`해`(`go`)와 `년`(`sus`)은 교정하지 않습니다.** 한글도 영어도 둘 다 자주 쓰는 말이면 건드리지 않는 것이 맞다고 판단했습니다.

### 수정

- **`만`·`좀`·`돼`·`들` 같은 글자가 목록에 있는데도 안 바뀌던 문제를 해결합니다.** 교정할 글자로 등록돼 있었지만 프로그램이 그 경로에서 목록을 확인하지 않아, 실제로는 한 번도 동작하지 않았습니다.

- **엑셀에서 셀을 옮긴 직후 첫 단어가 교정되지 않던 문제를 해결합니다.** 셀을 바꾸고 바로 치면 그 단어만 안 되고 잠깐 기다렸다 치면 됐습니다. 새 셀을 인식할 준비를 한 번만 하고 끝냈기 때문인데, 이제 짧은 간격으로 여러 번 시도합니다. 스프레드시트뿐 아니라 폼의 다음 칸으로 넘어갈 때도 함께 좋아집니다.

## [1.7] — 2026-07-25

### 변경

- **`좀`·`돼`·`만`·`들`처럼 자주 쓰는 한 글자가 이제 교정됩니다.** `돼 든 들 듯 등 딴 만 명 몇 백 열 편 좀 님 놈 곧 꽤` 17자가 새로 열렸습니다. 지금까지 안 되던 이유는 영어와 겹쳐서가 아니라, 프로그램 안에서 판단을 넘겨받을 곳이 없어 조용히 버려지고 있었기 때문입니다. 단어를 하나씩 추가한 것이 아니라 그 빈 처리를 없앴기 때문에, 앞으로 선언되는 한 글자도 같은 경로로 함께 열립니다. 영어 단어·명령어·로케일 코드와 겹치는 것을 막는 안전장치는 그대로입니다.

- **반대로 `해`(`go`)와 `년`(`sus`)은 교정하지 않도록 되돌렸습니다.** 한글도 영어도 둘 다 자주 쓰는 말이면 건드리지 않는 것이 맞다고 판단했습니다. `go `를 치면 `해 `가 되어 영어 문장이 깨졌고, `sus`는 요즘 영어에서 흔히 쓰는 데다 바뀐 결과가 욕설로 읽힐 수 있었습니다.

### 수정

- **엑셀에서 셀을 옮긴 직후 첫 단어가 교정되지 않던 문제를 해결합니다.** 셀을 바꾸고 바로 치면 그 단어만 안 되고, 잠깐 기다렸다 치면 됐습니다. 방향키·Enter·Tab으로 입력란을 옮길 때는 앱이 바뀌지 않아 준비 동작이 돌지 않았기 때문입니다. 마우스로 옮길 때는 이미 하던 준비를 키보드로 옮길 때도 하도록 맞췄습니다. 스프레드시트뿐 아니라 폼의 다음 칸으로 넘어갈 때도 같이 좋아집니다.

- **앱 목록에서 토글을 누르면 창이 그대로 닫히던 문제를 해결합니다.** 메뉴바에서 Mackor을 열고 `자판자동`이나 `조합` 스위치를 누르면 설정이 바뀌지 않고 패널이 닫혀서, 다시 열어 한 번 더 눌러야 했습니다. Mackor은 Dock에 뜨지 않는 메뉴바 앱이라 패널이 떠도 앱이 활성 상태가 되지 않았고, 그래서 첫 클릭이 스위치에 닿지 않고 앱을 깨우는 데 쓰인 뒤 패널이 사라졌습니다. 이제 패널을 열 때 앱이 함께 활성화되어 한 번에 눌립니다.

- **확인창의 버튼을 누르면 패널이 닫히던 문제를 해결합니다.** "자동 교정을 켤까요?" 같은 확인창에서 `켜기`를 누르든 `취소`를 누르든 패널이 닫혔습니다. 확인창이 별도의 창이라 그것이 닫힐 때 패널이 함께 사라졌기 때문입니다. 이제 확인창을 패널 안에 직접 그려서 창이 닫히는 일이 없습니다. 바깥 어두운 곳을 눌러도 취소되고, Enter·Esc 키도 그대로 동작합니다.

## [1.6] — 2026-07-25

### 변경

- **주소창에서도 한글 검색어를 교정합니다.** 지금까지 주소·URL 입력란은 통째로 제외돼 있었지만, 실측해 보니 URL 자체는 애초에 교정 대상이 되지 않습니다 — `naver.com`처럼 마침표가 들어가거나 `localhost:3000`·`my-site`처럼 기호·숫자가 섞이면 그 단어는 이미 후보에서 빠집니다(도메인 113개 실측 결과 오교정 0건). 이제 주소창에서도 스페이스·쉼표·물음표·느낌표 경계에서는 다른 입력란과 똑같이 교정합니다.

  다만 **Enter·Tab으로 제출할 때는 교정하지 않습니다.** 제출하면 페이지가 바로 넘어가 ⌘Z나 원문 되돌리기 버튼이 사라지기 때문입니다. 되돌릴 수 있는 곳에서만 고친다는 원칙입니다. 비밀번호·보안 입력란은 이 변경과 무관하게 이전과 똑같이 제외됩니다.

  알려진 동작: ⌘L로 주소창을 연 직후에는 주소 전체가 선택된 상태라 **첫 단어는 교정되지 않습니다.** 그다음 단어부터 정상 동작합니다.

### 수정

- **Chrome에서 자판 자동 교정이 켜지지 않던 문제를 해결합니다.** 지금까지 Chrome은 사용자가 앱 목록에 직접 추가하지 않으면 자판자동이 동작하지 않았습니다. Mackor은 앱을 전환한 뒤 한 번 접근성을 확인해 "동작 확인"으로 학습하는데, Chrome은 보조 기술이 실제로 조회를 시작해야 웹 접근성 트리를 **뒤늦게** 만들기 때문에 그 한 번의 확인에 언제나 걸리지 않았습니다(Chrome은 다른 앱에서 쓰는 활성화 속성도 받지 않습니다). 이제 확인을 한 번이 아니라 2.5초에 걸쳐 나눠 시도해, 트리가 늦게 서는 앱도 정상적으로 학습됩니다. 특정 앱 이름을 코드에 넣지 않았으므로 같은 방식으로 동작하는 브라우저·앱이 함께 열립니다.

- **겹받침을 지웠다가 다시 쓰면 글자가 깨지던 문제를 해결합니다.** `닭`을 친 뒤 백스페이스로 `달`이 된 상태에서 `ㄱ`을 다시 치면 `닭`이 아니라 `달ㄱ`이 됐습니다. `앉`·`값`·`읽`·`많` 등 모든 겹받침에서 같은 증상이었고, 스페이스를 치면 그대로 확정됐습니다.

- **오타를 지우고 잠시 멈춘 뒤 다시 치면 그 단어가 교정되지 않던 문제를 해결합니다.** 백스페이스로 단어를 전부 지운 다음 2초 넘게 쉬었다가 새로 입력하면, 새 단어가 통째로 교정 대상에서 빠졌습니다.

- **어퍼스트로피를 지운 직후 글자가 깨질 수 있던 문제를 해결합니다.** `it'`처럼 치고 백스페이스로 `'`만 지운 뒤 이어서 입력하면, 교정이 화면과 어긋난 글자 수를 지워 엉뚱한 문자열이 될 수 있었습니다.

- **비밀번호 칸에서 「AX 없이 강제 교정」이 드물게 동작할 수 있던 문제를 해결합니다.** 접근성 확인이 느려 한 번 실패한 뒤 곧바로 보호된 입력란으로 확인된 경우, 강제 교정이 취소되지 않고 그대로 실행될 수 있었습니다. 이 기능을 켜 둔 앱에서만 해당하며 이제 확인 즉시 취소됩니다.

- **일부 Apple 기본 앱에서 자판자동 토글이 켜지지 않던 문제를 해결합니다.** 목록에 보이는데도 켜자마자 다시 꺼진 상태로 돌아왔습니다.

## [1.5] — 2026-07-25

### 변경

- **"AX 없이 강제 교정" 토글을 켤 때 확인 창이 뜹니다.** 한컴오피스 한글처럼 접근성으로 글자를 읽을 수 없는 앱의 `자판자동`(강제 교정)은 화면을 확인하지 않고 지웠다 다시 쓰는 방식이라 드물게 글자를 잘못 지울 수 있습니다. 이제 켜기 전에 이 위험과 ⌘Z 복구를 명시한 확인 창을 띄우고, 확인해야 활성화됩니다(끄기는 즉시). 일반 자판자동을 미확인 앱에서 처음 켤 때와 같은 안전장치입니다.

## [1.4] — 2026-07-25

### 추가

- **운영자 공지·새 소식 알림.** 새로 지원되는 앱, 버그 수정, 공지 같은 소식이 있으면 메뉴바의 `Mackor`에 점(●)이 뜨고, 패널의 알림(종) 버튼이 부드럽게 깜빡입니다. 종을 누르면 새 소식 목록을 볼 수 있고, 열면 확인 처리되어 표시가 사라집니다. 공지는 업데이트와 같은 GitHub Pages에서 받아오며 **추적이나 식별자가 없고**, 설정의 `새 소식 확인`으로 끌 수 있습니다(끄면 네트워크 요청을 하지 않습니다). 같은 소식이 웹사이트(`https://keilkim.github.io/MacKoreanImefixer/`)에도 함께 표시됩니다.

### 변경

- 릴리스·자동 업데이트 배포의 안전장치를 강화했습니다(설치·사용 동작에는 변화가 없습니다). 자동 업데이트 피드가 과거 버전 항목을 잃지 않도록 보존하고, 게시 후 자산·서명·다운로드 링크를 원격에서 자동 재검증합니다. 버그 신고·기능 제안·보안 취약점 신고 경로를 저장소에 정비했습니다.

## [1.3] — 2026-07-25

첫 공식 Mackor 릴리스입니다. Developer ID 서명·Apple 공증·Sparkle EdDSA 서명을 모두 통과해 게시되었습니다.

### 추가

- **한컴오피스 한글처럼 접근성(AX)에 텍스트를 안 내놓는 앱에서도 자판자동을 쓸 수 있게 하는 "AX 없이 강제 교정"을 추가합니다.** 한컴 본문은 자체 캔버스라 AX로 글자를 읽을 수 없어 원래는 자판자동이 불가능했습니다. 다만 실측 결과 한컴은 합성 입력(타이핑·백스페이스)은 받아들이므로, 미지원 앱의 `자판자동` 토글을 켜면 화면을 읽지 않고 **검증 없이** 지우고 다시 쓰는 방식으로 교정합니다(위험을 아는 사용자용 opt-in, ⌘Z로 복구). 영→한·한→영 양방향, 교정 후 입력 소스 전환까지 동작합니다. 앱 이름을 코드에 넣지 않고 "AX 없음"이라는 공통 조건으로 판정하므로 같은 부류 앱이 함께 열립니다.

- **`it's`·`don't`·`we're` 같은 영어 축약형이 한글 모드에서 안 고쳐지던 문제를 해결합니다.** 한글 자판으로 축약형을 치면 `ㅑㅅ'ㄴ`처럼 자모 사이에 어퍼스트로피가 들어가는데, 지금까지는 그 어퍼스트로피가 교정 후보를 통째로 버려서 손도 못 댔습니다. 이제 어퍼스트로피를 판정 기준이 아니라 **투명한 구분자**로 보고, 양옆 글자를 합쳐 영어로 읽었을 때 말이 되는지 확인해 교정합니다. `배고프다'라고`·`세상'이라는`처럼 한국어 따옴표+조사는 합쳐 읽어도 정상 한글이라 그대로 보존됩니다.

- **`뭐야`·`뭐해`·`미쳤다`·`개꿀`처럼 구어체가 영어로 남던 문제를 규칙으로 해결합니다.** 영자판 상태로 `anjdi`(뭐야)를 치면 지금까지는 "영어로도 그럴듯한 형태"로 판정돼 그대로 남았습니다. 영어 정서법 규칙 두 개를 추가했습니다 — ① j 뒤에는 반드시 모음이 온다(jam·inject; 사전 23.6만 단어 중 위반은 raj류 차용어 52개뿐), ② 어중 대문자는 영어에 없다(두벌식 어중 Shift 쌍자음이 여기 걸립니다). 단어를 나열하지 않고 규칙 두 개로 뭐야·뭐해·뭐냐·뭐래·뭔데·미쳤다·재밌다·개꿀·핵꿀 계열 전체가 해결되며, 오탐은 하나도 늘지 않았습니다(벤치마크 87→87, 재현율 98.03%→98.08%).

- **한글 자판으로 `card`를 친 `ㅊㅁㄱㅇ`이 그대로 남던 문제를 해결합니다.** 서로 다른 자음 자모 나열은 `ㅁㄴㅇ` 같은 키보드 매시로 보호되는데, `card`·`water`·`great`처럼 자음 자판 글자만으로 적힌 실제 영어 단어까지 함께 묶여 있었습니다. 이제 4글자 이상이고 결과가 영어 사전에 실단어로 있을 때만 영어로 되살립니다. `asd`·`asdf` 같은 진짜 매시와 3글자 초성체(`ㅁㅊㄷ` 등)는 그대로 보호됩니다. 구제된 교정은 원문 칩이 6초 내내 강조 표시돼 한 번에 되돌릴 수 있습니다.

- **한 글자짜리 한글이 영어로 남던 문제를 해결합니다.** `로`·`요`·`를`·`는`·`아`·`네`·`오`처럼 자주 쓰는 한 글자를 영자판 상태로 치면 지금까지는 그대로 `fh`·`dy`·`fmf` 로 남아 있었습니다. 한 글자는 근거가 약해 프로그램이 판단을 포기하도록 설계돼 있었기 때문입니다. 이제 자주 쓰는 한 글자 79개를 고정 목록으로 확인해 교정합니다. `SK`·`DNS`·`SMS` 같은 약어와 `go`·`an`·`rm` 같은 짧은 영어는 그대로 둡니다.

- **한글로 써야 맞는 단어가 영어로 남던 문제를 사전으로 해결합니다.** `만들다`(aksemfek), `캐나다`(zoskek), `배관`(qorhks), `문맥`(ansaor), `내가`(sork)처럼 한글로는 실제 단어인데 영어로 읽으면 아무 뜻이 없는 입력이, 지금까지는 규칙만으로 판단할 수 없어 영어로 남아 있었습니다. 이제 두 언어의 사전을 함께 보고 한쪽만 실제 단어면 그쪽으로 확정합니다. `work`·`ahem`처럼 영어로도 진짜 단어인 경우는 그대로 둡니다.
- 앱 메뉴에서 새 버전과 변경 내용을 확인할 수 있는 업데이트 기능
- 처음 설치할 때 터미널 없이 더블클릭할 수 있는 macOS 설치 패키지

### 수정

- **뜻 없는 한 글자로 바꿔버리던 문제.** `cma`(CMA)가 `츰`으로, `aht`가 `못`으로, `dha`가 `옴`으로 바뀌고 있었습니다. 한 글자 교정을 허용할지 판단하는 기준이 "영어 모음이 들어 있는가"였는데, 두벌식에서 영어 모음 자리는 한글 자모 ㅁ·ㄷ·ㅑ·ㅐ·ㅕ 라서 그 기준이 실제 단어인지와 아무 관계가 없었습니다. 이제 고정 목록에 있는 글자만 바꿉니다.

- **실행 중 손쉬운 사용 권한을 끄면 Mac 전체 입력이 멈춘 채 풀리지 않던 문제.** 권한이 회수되면 입력을 가로채던 통로가 무효화되는데, 그 사실을 알아채는 경로가 앱 전환에만 묶여 있었습니다. 전환하려면 입력이 필요한데 그 입력이 이미 멈춰 있어 스스로 회복할 수 없었습니다. 이제 입력과 무관하게 주기적으로 상태를 확인해 무효화된 통로를 즉시 떼어냅니다.
- **자동 교정 범위를 "전체 Mac"으로 두면 한글 조합 보정이 꺼지던 문제.** 등록한 앱에서 조합 보정을 켜뒀는데도 전체 Mac 모드에서는 아무 안내 없이 동작하지 않았습니다. 이제 두 기능을 동시에 쓸 수 있습니다 — 모든 앱에서 한/영 자동 교정이 동작하면서, 등록한 앱에서는 한글 조합 보정도 함께 동작합니다.
- 조합 보정 중 백스페이스를 섞어 입력하면 화면과 내부 기록이 어긋나 교정이 글자를 잘못 지울 수 있던 문제. 해당 단어의 자동 교정을 건너뛰도록 했습니다.
- 앱을 전환할 때 이전 앱에서 차단했던 키 정보가 남아 다음 입력이 삼켜질 수 있던 문제.

### 변경

- 양방향 오입력을 단어별 하드코딩 없이 macOS 영·한 사전의 비대칭 근거로 판정하고, authoritative target이 단순 철자 heuristic보다 우선하도록 통일
- 영문 사전의 원형 대소문자를 먼저 확인하고, 사전의 확정 hit가 단순 철자 휴리스틱보다 우선하도록 개선
- 토큰 전체의 단일 자모 반복은 보호하면서 정상 영문 double-letter는 복구할 수 있도록 자모 규칙 정밀화
- 사전을 호출하지 않은 판정과 호출했지만 사용할 수 없던 판정을 진단에서 구분
- 첫 공식 Mackor 기준선의 앱 이름·경로·번들 ID를 `Mackor`, `/Applications/Mackor.app`, `com.mackor.app`으로 통일

### 보안

- 공식 배포 경로에서 Apple 공증과 Sparkle EdDSA 검증을 필수 gate로 추가

### 알려진 제한

- legacy MacKR·CorelHangulFix 설치, 설정, 손쉬운 사용 권한은 아직 자동 이전하지 않음

### 규칙 기반 교정 엔진 (Rule-Based Correction Engine)

한/영 오입력 교정의 판단 근거를 **사전 조회에서 문법 제약 충족으로 전면 교체**했습니다.

#### 왜

기존 엔진의 한→영 승인 조건은 "macOS 영어 사전이 인정 ∧ 한국어 사전이 거부"의 AND였습니다.
실측 결과 macOS 한국어 맞춤법 검사기는 거의 모든 단어를 인정하고(안녕/커피/회의/프로젝트…),
영어 검사기는 실무 어휘를 거부합니다(github, kubernetes). 이 AND는 구조적으로 성립할 수
없었고, 한→영 교정이 되지 않던 것은 버그가 아니라 설계의 필연이었습니다.

#### 무엇이 바뀌었나

- **사전 0, 시스템 서비스 0.** 번들 사전 143단어와 `NSSpellChecker` 의존(8ms 타임아웃,
  500ms 쿨다운, XPC 헬스체크 ~240행)을 전부 제거했습니다.
- **두벌식·영어 문법을 각각 채점하고 마진으로 결정합니다.** 한국어는 조합 오토마톤 +
  KS X 1001 현대 음절(2,350자, OS 인코딩 변환기에서 런타임 유도) + 음절 구조 제약으로,
  영어는 공명도 위계 + 정서법 제약으로 판정합니다. 자음군 목록조차 두지 않고 생성합니다.
- **하드 veto 4종 제거.** 2글자 단어(`we`, `see`), 대문자 시작, 반복 문자가 포함된 단어가
  다시 교정됩니다. 전대문자는 약어 표기 관례 규칙으로 보존됩니다(`DHCP`, `NASA`).
- **Caps Lock**이 영문 자판에서 더 이상 교정을 중단시키지 않습니다.

#### 측정 (재현: `python3 -B scripts/rulebench/bench.py`)

| 방향 | 재현율 | 오탐 |
|---|---|---|
| 영자판→한글 복원 | 98.03% (19,895 실어절) | 87/198,485 = 0.044% |
| 한글자판→영어 복원 | 97.45% (198,485 단어) | 0/19,895 |

기존 회귀 코퍼스 `tune.tsv` 23/23, `holdout.tsv` 27/27을 근거 주입 없이 통과합니다.
신규 `Corpus/structure-correction/v2/golden.tsv`(405행)가 Swift 구현과 파이썬 레퍼런스의
행 단위 일치를 강제합니다.

#### 알려진 한계

`world↔재깅`, `모든↔ahems`처럼 두 문법을 모두 만족하는 토큰은 키열만으로 구분할 수 없어
보존합니다(영어 표본의 약 2.9%). 소문자 약어 `dhcp`는 `사랑`(tkfkd)과 구조가 완전히
동일해 여전히 교정됩니다 — 원문 칩과 ⌘Z로 복구할 수 있습니다.
자세한 내용은 `STRUCTURE_CORRECTION_DESIGN4.md` §5를 참고하세요.

#### 제출 경계 (Enter / Tab)

Space·쉼표는 경계 문자를 앱에 먼저 보낸 뒤 지우고 다시 넣는 방식이라 Enter에 쓸 수
없었습니다. 메시지가 이미 전송된 뒤에 빈 입력창을 고치게 되기 때문입니다. 그래서
채팅·검색·커밋 메시지처럼 **한 단어 치고 바로 Enter**를 누르는 가장 흔한 상황에서
교정이 전혀 동작하지 않았습니다.

v1.3부터 Return·숫자패드 Enter·Tab에서 흐름을 뒤집어 처리합니다.

- 교정할 후보가 **있을 때만** 키를 붙잡고, 원문을 고친 뒤 그 키를 주입합니다.
  후보가 없으면 키를 그대로 흘려보내므로 대다수 입력의 동작은 그대로입니다.
- 포커스가 어긋나 교정을 포기하더라도 **Enter는 반드시 주입합니다.** 사용자의 Enter를
  삼키는 것이 잘못 교정하는 것보다 나쁩니다.
- `Shift+Tab`은 역방향 포커스 이동이므로 경계로 쓰지 않습니다.

#### 원문 칩의 신뢰 등급 반영

`medium` 등급 교정(반대 읽기도 구조적으로 가능했던 경우)은 칩을 강조색 테두리와
함께 표시하고, 수명을 ⌘Z 트랜잭션과 같은 6초로 맞췄습니다. 칩은 사라졌는데
되돌리기는 아직 되는 간극이 없어집니다.

[Unreleased]: https://github.com/Keilkim/MacKoreanImefixer/compare/v1.9...HEAD
[1.9]: https://github.com/Keilkim/MacKoreanImefixer/compare/v1.8...v1.9
[1.8]: https://github.com/Keilkim/MacKoreanImefixer/compare/v1.7...v1.8
[1.7]: https://github.com/Keilkim/MacKoreanImefixer/compare/v1.6...v1.7
[1.6]: https://github.com/Keilkim/MacKoreanImefixer/compare/v1.5...v1.6
[1.5]: https://github.com/Keilkim/MacKoreanImefixer/compare/v1.4...v1.5
[1.4]: https://github.com/Keilkim/MacKoreanImefixer/compare/v1.3...v1.4
[1.3]: https://github.com/Keilkim/MacKoreanImefixer/compare/v1.1...v1.3
